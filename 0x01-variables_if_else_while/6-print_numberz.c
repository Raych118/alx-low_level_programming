#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int y;

	for (y = 48 ; y < 58 ; y++)
	{
		putchar(y);
	}
	putchar('\n');
	return (0);
}
